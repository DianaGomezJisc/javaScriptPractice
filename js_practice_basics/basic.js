/**
 * Node.js is a runtime environment for running JavaScript code.
 * Used for building backend services (Apis)
 * Large ecosystems of opensource libs
 *
 * NPM: is a package manager that makes installing Node packages fast and easy
 * package is a module - code library that extends Node by adding useful features
 * It is usually installed when installing Node.js
 *
 * MAC  brew install node
 *      npm -v
 * On VisualStudio you can add plugins, i.e. testCafe snipers
 * npm install testcafe
 * npm init
 * package.json should be installed by now
 *
 */
